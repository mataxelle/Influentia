{% extends 'base.html.twig' %}

{% block title %}{{ parent()}} | Prenium{% endblock %}

{% block body %}
<div>
    <h1>Hello All prenium post !</h1>

    <a href="{{ path('post_add') }}">Ajouter un post prenium</a>

    <article>
    {% for preniumPost in preniumPosts %}
        <h2>{{ preniumPost.title }}</h2>
        <a href="{{ path('prenium_ post_show', {'id' : preniumPost.id}) }}">par ici</a>
    {% endfor %}
    </article>
</div>
{% endblock %}
