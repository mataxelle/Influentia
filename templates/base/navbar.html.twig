<header>
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<div class="container-fluid">
			<a class="navbar-brand ms-5" href="{{ path('index') }}">Influentia
			</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse me-5" id="navbarSupportedContent">
				<ul class="navbar-nav ms-auto text-center">
					<li class="nav-item active">
						<a class="nav-link active" href="{{ path('index') }}">Accueil<span class="sr-only"></span>
						</a>
					</li>
					<li class="nav-item active">
						<a class="nav-link" href="{{ path('prenium') }}">Prenium<span class="sr-only"></span>
						</a>
					</li>
					<li class="nav-item active">
						<a class="nav-link" href="{{ path('cart') }}">Panier<span class="sr-only"></span>
						</a>
					</li>
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="#">
								Admin
							</a>
							<ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
								<li>
									<a class="dropdown-item" href="{{ path('admin') }}">Dashboard</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('post_add') }}">Ajouter article</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('prenium_post_add') }}">Ajouter Prenium</a>
								</li>
							</ul>
						</li>
					{% endif %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="#">
							Profil
						</a>
						<ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
							{% if app.user %}
								<li>
									<a class="dropdown-item" href="{{ path('account', {'id': app.user.id}) }}">Mon compte</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('logout') }}">Deconnexion</a>
								</li>
							{% elseif app.user is null %}
								<li>
									<a class="dropdown-item" href="{{ path('login') }}">Connexion</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('register') }}">Inscripion</a>
								</li>
							{% endif %}
						</ul>
					</li>
				</ul>
			</div>
		</div>
	</nav>
</header>
